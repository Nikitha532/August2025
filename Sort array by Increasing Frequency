class Solution(object):
    def frequencySort(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        dict ={}
        for i in nums :
            if i in dict :
                dict[i] = dict[i]+1
            else :
                dict[i] = 1
        s_d = sorted(dict.items(),key = lambda x:(x[1],-x[0]))
        result = []
        for key,value in s_d :
            for i in range (value) :
                result.append(key)
        return result
