class Solution(object):
    def isAnagram(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: bool
        """
        str_s = "".join(sorted(s))
        str_t = "".join(sorted(t))
        if str_s == str_t :
            return True
        return False
